(* open Opium

(* Type of the note. Should obviously store the content of the note, as some extra stuff
to allow the note to be retrieved, like an id. Should be json compatible. *)
type note = { id: int; title: string; content: string}

(* Retrive notes from the store *)
val get_all_notes : _ -> note list

(* Should send the html page that displays all of the users notes *)
val get_all_notes_handler : Request.t -> Response.t Lwt.t

(* Retrieves a single note from the store *)
val get_note : int -> note

(* Should send the html page that shows a note and allows for editing *)
val get_note_handler : Request.t -> Response.t Lwt.t

(* Deletes a note from the store *)
val delete_note : int -> _

(* Handles delete requests for a note *)
val delete_note_handler : Request.t -> Response.t Lwt.t

(* Updates a note *)
val update_note : int -> string -> string -> _

(* Handles update note requests *)
val update_note_handler : Request.t -> Response.t Lwt.t

(* Creates a new note and stores it *)
val create_note : string -> string -> _

(* Handles requests to the server for creating a new note *)
val create_note_handler : Request.t -> Response.t Lwt.t *)
